# Support Policy

**Status:** replacement support policy.

Supported repositories are replacement-protocol repositories that validate
against the active schema family. Archived v0.3/v1 repositories are historical
and unsupported by the active runtime.

Support responses must not promise automatic migration, transparent fallback,
or dual-protocol compatibility.
